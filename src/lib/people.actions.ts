/**
 * Actions
 * - Delete a deletePerson
 */
import { createAction, props } from "@ngrx/store";

export class PeopleActions {
    /*public deletePerson = createAction(
        '[People Page] Delete Person',
        props<{id: string}>());*/
}
